# CV Processor Lambda dependencies
# Core extraction (no cryptography dependencies)
python-docx>=0.8.11
pdfplumber>=0.10.3
Pillow>=10.0.0

# OCR (for triple OCR fallback - Tesseract requires system install)
# Note: pytesseract and pdf2image removed - using Tesseract layer directly
# Note: cohere removed - embeddings handled by cv_parser Lambda

# AWS SDK (provided by Lambda runtime, but pinned for local dev)
boto3>=1.34.0
